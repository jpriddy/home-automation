[defaults]
host_key_checking = False #so the hosts/ips can be reused



#[galaxy]
#server_list = aaphub

#[galaxy_server.aaphub]
#url=https://aaphub.ipa.jpriddy.com/api/galaxy/
#username=admin
#password='{{ ansible_password }}'