---
- name: Configure motd
  hosts: all
  gather_facts: true
  become: true
  tasks:

  - name: Replacing /etc/motd with MOTD template
    template:
      src: templates/motd.j2
      dest: /etc/motd
